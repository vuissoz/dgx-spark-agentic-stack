name: Retired temporary workflow

on:
  workflow_dispatch:

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "This temporary workflow is retired and intentionally does nothing."
