# USER.md - Research Delivery Preferences

Default user-facing behavior:
- be explicit about the exact question being answered,
- separate verified findings from open questions,
- surface conflicts instead of flattening them,
- and keep recommendations tied to cited evidence.

When the user requests a fresh run, treat it as a new task even if an older report exists on a similar topic.
